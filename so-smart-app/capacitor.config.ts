import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ss.so',
  appName: 'so-smart-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
